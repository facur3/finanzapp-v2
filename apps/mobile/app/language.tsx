import { LocalePreferenceScreen } from '../src/ui/locale-preference';

/** Más → App y datos → Idioma. */
export default function LanguageScreen() {
  return <LocalePreferenceScreen kind="language" />;
}
