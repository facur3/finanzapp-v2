import type { Messages } from '../../messages.ts';

/** Reportes: periods, charts, comparisons, the category and day screens. */
export const reports: Pick<Messages, 'reports'> = {
  reports: {
  },
};
