{
  "name": "finanzapp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check:repo": "node scripts/check-repo.mjs",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
